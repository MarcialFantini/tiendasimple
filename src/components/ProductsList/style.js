import styled from "styled-components";

export const MosaicoGrid = styled.div`
    display: grid;
    grid-template-columns: repeat(auto-fill,minmax(130px,1fr));
    gap: 10px;

    justify-items: center;
    align-items: center;

`